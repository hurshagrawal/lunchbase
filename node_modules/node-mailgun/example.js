var mailgun = require('./index.js');

// Configure mailgun.
mailgun('api-key-goes-here')

// Build message.
.add('host', 'example.mailgun.org')
.add('from', 'postmaster@example.com')
.add('to', 'me@example.com')

// You can use key/val pairs, or an object.
.add({
  'subject': 'Some subject...'
  , 'body': "This is\nsome body."
})

// Attempt to send. Callback is optional.
.send(function(err){
  console.log(err ? err : 'success!');
});

// OR

// Configure mailgun.
mailgun({
  key: 'api-key-goes-here'
  , host, 'example.mailgun.org'
  , from, 'postmaster@example.com'
  , to, 'me@example.com'
  , subject: 'Some subject...'
  , body: "This is\nsome body."
})

// Attempt to send. Callback is optional.
.send(function(err){
  console.log(err ? err : 'success!');
});